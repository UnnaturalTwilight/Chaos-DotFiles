(include "activate.yuck")

(include "bar.yuck")

;; plans for future widgets
; (include "power.yuck")
; (include "music.yuck") 
; (include "blutooth.yuck") ; Possably just install a gui app for this
(include "sysmonitor.yuck") ; just a little resource monitor

;; Common Widgets 

(defwidget padding [space ?expand]
    (box :class "gap" :orientation "h" :halign "fill" :width space :hexpand expand))